#pragma once

namespace Mylib
{
	void sortingDar(int* dar, int size);
	void printDar(int* dar, int size);
	float* create_InitDar(short size);
	void pol(float* dar, short size, int *pol);
	void min(int* dar, short size, int* min);

	void printTrueandFalse(float* dar, short size);
	
	

	
	
}
